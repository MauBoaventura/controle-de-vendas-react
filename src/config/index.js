// export const getBaseURL = async () => {
  // return "http://localhost:3035";
  // if (process.env.REACT_APP_DEBUG === "true") {
  //   if (window.location.href.includes("localhost")) {
  //   } else {
  //     const municipio = window.location.href.replace("//", ".").split(".")[1];
  //     return `http://api.desenvolvimento.pege.com.br/api/${municipio}/aluno`;
  //   }
  // } else {
  //   const municipio = window.location.href.replace("//", ".").split(".")[1];
  //   return `http://exercicios.pege.com.br/api/${municipio}/aluno`;
  // }
// };

// export const getClient = () => {
//   return window.location.href.replace("//", ".").split(".")[1];
// };
